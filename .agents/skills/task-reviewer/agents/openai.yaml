interface:
  display_name: "Task Reviewer"
  short_description: "Run a local, spec-aware task review"
  default_prompt: "Use $task-reviewer to review the current implementation locally against its full task package and exact diff."
